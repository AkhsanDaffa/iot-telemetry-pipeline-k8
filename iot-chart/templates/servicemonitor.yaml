apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ .Release.Name }}-iot-monitor
  labels:
    app: iot-sensor
    release: monitoring
spec:
  selector:
    matchLabels:
      app: iot-sensor
  endpoints:
  - port: metrics
    interval: 5s
    path: /metrics